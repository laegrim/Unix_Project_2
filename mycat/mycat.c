#include <stdio.h>
#include <unistd.h>

static void read_input_to_output(char * filepath, bool raw){

}

int main(int argc, char ** argv){
	
	//get any options passed by command line
	while ((int opt = getopt(argc, argv, "")) != -1) {
		
		//detect which options, use flags
		switch (opt) {
			case :

			default: 
		}

	}
	//get the remaining non option arguments
	
	//determine where the input is coming from
	//stdout vs file
	
	//exit gracefully

}	
